/**
 * Page: /constraints
 * 03 Constraints — standalone page wrapper
 */
import ConstraintsSection from "@/pages/home/Constraints";

export default function Constraints() {
  return <ConstraintsSection />;
}
