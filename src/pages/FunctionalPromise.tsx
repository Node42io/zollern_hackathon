/**
 * Page: /functional-promise
 * 02 Functional Promise — standalone page wrapper
 */
import FunctionalPromiseSection from "@/pages/home/FunctionalPromise";

export default function FunctionalPromise() {
  return <FunctionalPromiseSection />;
}
